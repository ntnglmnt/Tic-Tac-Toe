const gameBoard = {
    board: [a, b, c, 
            d, e, f, 
            g, h, i]
};

function Player(name, mark) {
    this.name = name;
    this.mark = mark;
}

const playerOne = new Player(John, X)
const playerTwo = new Player(Doe, O)

